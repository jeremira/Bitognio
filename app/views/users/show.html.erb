<section class="content-block container-fluid">
  <div class="row">
    <div class="col-xs-12 col-md-6 offset-md-3">
      <h4 class="separate-red">
        <span class="fa-stack color-red">
          <i class="fa fa-square-o fa-stack-2x"></i>
          <i class="fa fa-home fa-stack-1x"></i>
        </span>
        <%= t('.welcome', email: current_user.email) %>
      </h4>
      <% if current_user.is_a_teacher %>
        <p>is a teacher </p>
      <% else %>
        <p>is a student </p>
      <% end %>
      <%= link_to t(:__new_payment), '#' %> | <%= link_to t(:payments_history), '#' %>
    </div>
  </div>
</section>
